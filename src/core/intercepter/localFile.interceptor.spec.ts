import { IntercepterInterceptor } from './intercepter.interceptor';

describe('IntercepterInterceptor', () => {
  it('should be defined', () => {
    expect(new IntercepterInterceptor()).toBeDefined();
  });
});
